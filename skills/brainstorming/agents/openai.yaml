interface:
  display_name: "Brainstorming"
  short_description: "Generate, compare, and structure ideas with explicit constraints and tradeoffs"
  default_prompt: "Use $brainstorming to generate options, capture constraints, and compare tradeoffs before we commit to a direction."
