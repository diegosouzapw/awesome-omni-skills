interface:
  display_name: "API Design"
  short_description: "Design stable HTTP and service contracts before implementation"
  default_prompt: "Use $api-design to define resources, envelopes, errors, compatibility rules, and example packets for this API."
