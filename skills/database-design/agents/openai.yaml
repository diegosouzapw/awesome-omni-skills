interface:
  display_name: "Database Design"
  short_description: "Design schemas, indexes, migrations, and operational data boundaries"
  default_prompt: "Use $database-design to define entities, relationships, access paths, and migration safety for this data model."
