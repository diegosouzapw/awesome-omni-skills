interface:
  display_name: "Architecture"
  short_description: "Define system boundaries, tradeoffs, and phased implementation plans"
  default_prompt: "Use $architecture to define boundaries, contracts, rollout phases, and tradeoffs for this system change."
