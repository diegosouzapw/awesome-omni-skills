interface:
  display_name: "Debugging"
  short_description: "Reproduce bugs, narrow fault domains, and verify fixes with evidence"
  default_prompt: "Use $debugging to reproduce the issue, test competing hypotheses, and verify the final fix with evidence."
